{
  "name": "balancer-deprecated-exit-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "discover": "node scripts/discover.mjs",
    "discover:deprecated": "node scripts/discover.mjs --chain mode && node scripts/discover.mjs --chain fraxtal && node scripts/discover.mjs --chain zkevm"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^8.2.1"
  }
}
